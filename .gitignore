# Terraform
**/.terraform/
**/*.tfstate
**/*.tfstate.*
**/*.tfvars
!**/*.tfvars.example
# Keep the lock file - ensures consistent provider versions across machines
# **/.terraform.lock.hcl

# Kubeconfig / credentials
*.kubeconfig
kubeconfig

# direnv - .envrc files often contain secrets; manage per-directory
# (add per-directory .envrc to .gitignore if they contain real values)
**/.envrc

# Terraform temporary files written during apply
**/*.tmp

# Generated local env overrides (written by scripts/update-env.sh)
**/.env.local

# macOS
.DS_Store
